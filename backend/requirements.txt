# VoiceShield backend dependencies
# ML model stack (Transformers, SpeechBrain, Torchaudio, ECAPA-TDNN,
# Wav2Vec2) is intentionally excluded and added in its own phase.
# Librosa is used for high-quality resampling and silence trimming; it
# pulls in numpy/scipy/numba/scikit-learn transitively.

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10

# Validation / settings
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# File upload parsing (required by FastAPI UploadFile/File)
python-multipart==0.0.20

# Audio preprocessing (Phase 3)
numpy==2.5.3
soundfile==0.14.0
librosa==1.0.0
soxr==1.1.0
# Bundles a static FFmpeg binary used as a fallback when FFmpeg is not on PATH
imageio-ffmpeg==0.6.0

# ML inference (deepfake / synthetic speech detection)
# CPU build (auto-set by pip on Windows/PyPI). For GPU support, install a
# CUDA-enabled build instead, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
torch==2.14.0
transformers==5.16.1

# Testing
pytest==8.3.4
httpx==0.28.1
